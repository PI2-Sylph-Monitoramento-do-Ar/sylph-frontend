import React from "react";
import {{pascalCase name}} from "./{{pascalCase name}}";

describe('{{pascalCase name}} tests', () => {
  it('has a test that should not pass for now', () => {
    expect(true).toBe(false);
  })
})